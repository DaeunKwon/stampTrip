.env
node_modules
dist
.vercel
docs
font

dist-e2e
e2e-results
.DS_Store

# Capacitor 네이티브 빌드 산출물 (android/.gitignore 가 대부분 처리, local.properties 는 개인 SDK 경로)
android/local.properties
android/app/release/
ios/App/App/public

# 릴리스 서명 키 (원스토어 업로드용) — 절대 커밋 금지, 별도 백업
android/keystore/
android/keystore.properties

# 스토어 등록 자료 (스크린샷 · 배너 · 아이콘 초안 · 등록 가이드 md) — 로컬 보관, 다시 만들려면 npm run store:shots / store:banner
store/
